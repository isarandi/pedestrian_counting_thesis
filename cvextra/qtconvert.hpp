//#ifndef QTCONVERT_H
//#define QTCONVERT_H

//#include <QImage>
//#include <QApplication>
//#include <QPixmap>
//#include <opencv2/opencv.hpp>

//namespace cvx { namespace qt {

//auto toMat(QImage const& image, bool cloneImageData = true) -> cv::Mat;
//auto toMat(QPixmap const& pixmap, bool cloneImageData = true) -> cv::Mat;

//auto toQImage(cv::InputArray src) -> QImage;
//auto toQPixmap(cv::InputArray src) -> QPixmap;

//auto toQColor(cv::Scalar colorBGR) -> QColor;

//}}


//#endif // QTCONVERT_H
